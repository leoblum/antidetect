{
  "name": "antidetect",
  "private": true,
  "scripts": {
    "start": "lerna run start --stream",
    "test": "lerna run test --stream",
    "cloc": "cloc --exclude-dir=.idea,node_modules --not-match-f='(package.json|package-lock.json)' ."
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
